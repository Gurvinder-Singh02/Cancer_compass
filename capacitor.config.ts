import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tech.Cancercompassapp',
  appName: 'Health Cohart',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
